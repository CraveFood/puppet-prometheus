---
version: 4

datadir: data
hierarchy:
  - name: 'Distribution Name'
    path: "%{facts.os.name}.yaml"
    backend: yaml
  - name: 'Operating System Family'
    path: "%{facts.os.family}.yaml"
    backend: yaml
  - name: 'defaults'
    backend: yaml
